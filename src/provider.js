/*
import * as firebase from 'firebase'

firebase.initializeApp(configuracion que entrega firebase);

export const database=firebase.database();
*/

/*si deseo hacer un auth agregar al principio de inde.js
componentDidMount(){
 database.ref('pedidos').on('value',(snapshot)=>{ 
     this.setState({
         ...this.state,
         pedidos:snapshot
     });
 })
 }
*/